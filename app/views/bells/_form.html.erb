<%= form_for(@bell) do |f| %>
  <% if @bell.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bell.errors.count, "error") %> prohibited this bell from being saved:</h2>

      <ul>
      <% @bell.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
