<%= form_for(@game_source) do |f| %>
  <% if @game_source.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@game_source.errors.count, "error") %> prohibited this game_source from being saved:</h2>

      <ul>
      <% @game_source.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
