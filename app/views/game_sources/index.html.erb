<h1>Listing game_sources</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @game_sources.each do |game_source| %>
  <tr>
    <td><%= link_to 'Show', game_source %></td>
    <td><%= link_to 'Edit', edit_game_source_path(game_source) %></td>
    <td><%= link_to 'Destroy', game_source, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Game source', new_game_source_path %>
